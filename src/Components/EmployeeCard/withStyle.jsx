import styled from 'styled-components';


export default (component) => styled(component)`
    .card {
        max-width: 300px;
    }
    .media {
        height: 300px;
    }
    .check-box {
        padding: 1px;
    }
`;
